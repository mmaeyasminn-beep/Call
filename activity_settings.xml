<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_page">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView style="@style/SectionTitle" android:text="الاتصال والنغمات" />

        <LinearLayout style="@style/SettingsCard">
            <LinearLayout android:id="@+id/row_general_ringtone" style="@style/SettingsRow">
                <TextView style="@style/SettingsLabel" android:text="@string/settings_general_ringtone" />
                <TextView android:id="@+id/value_general_ringtone" style="@style/SettingsValue" />
            </LinearLayout>

            <LinearLayout android:id="@+id/row_default_line" style="@style/SettingsRow">
                <TextView style="@style/SettingsLabel" android:text="@string/settings_default_line" />
                <TextView android:id="@+id/value_default_line" style="@style/SettingsValue" />
            </LinearLayout>

            <LinearLayout android:id="@+id/row_line1_ringtone" style="@style/SettingsRow">
                <TextView style="@style/SettingsLabel" android:text="@string/settings_line1_ringtone" />
                <TextView android:id="@+id/value_line1_ringtone" style="@style/SettingsValue" />
            </LinearLayout>

            <LinearLayout android:id="@+id/row_line2_ringtone" style="@style/SettingsRow">
                <TextView style="@style/SettingsLabel" android:text="@string/settings_line2_ringtone" />
                <TextView android:id="@+id/value_line2_ringtone" style="@style/SettingsValue" />
            </LinearLayout>
        </LinearLayout>

        <TextView style="@style/SectionTitle" android:text="أثناء المكالمة" />

        <LinearLayout style="@style/SettingsCard">
            <LinearLayout style="@style/SettingsRow">
                <TextView style="@style/SettingsLabel" android:text="@string/settings_auto_record" />
                <Switch android:id="@+id/switch_auto_record" android:layout_width="wrap_content" android:layout_height="wrap_content" />
            </LinearLayout>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="12dp"
                android:textSize="11sp"
                android:textColor="@color/text_faint"
                android:text="تنبيه: بعض إصدارات أندرويد (10 فأعلى) بتمنع تسجيل صوت المكالمة مباشرة لأسباب خصوصية من جوجل. التسجيل ممكن ميشتغلش على كل الأجهزة." />
        </LinearLayout>

    </LinearLayout>
</ScrollView>
